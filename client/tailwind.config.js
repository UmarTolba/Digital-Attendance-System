
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./*.html", // Search all HTML files in the root directory
    "./src/**/*.{js,jsx,ts,tsx}", // Search all HTML/JS files in the 'src' directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}